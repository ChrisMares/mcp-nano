# Markdown Sample

This is a small markdown sample used to verify the `.md` document loader
preserves heading text and the body.

- Bullet one
- Bullet two with `inline code`.

```python
def hello():
    print("hi")
```